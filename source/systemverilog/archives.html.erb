<h1>Archives</h1>

x<% blog('systemverilog').articles.group_by {|a| a.date.year }.each do |year, articles| %>
  <h2><%= year %>
    <%= #link_to "#{year} (#{articles.size})", blog_year_path(year) 
    %>
  </h2>
  <ul>
    <% page_articles('systemverilog').each_with_index do |article, i| %>
      <% next if article.data.layout != 'article' %>
      <li><%= link_to article.title, article %> <time><%= article.date.strftime('%b %e') %></time></li>
    <% end %>    
  </ul>
<% end %>
